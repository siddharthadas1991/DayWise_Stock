@EndUserText.label: 'Day-Wise Material Stock – OData V4 Binding'

/*
 * ZMATL_DAYSTOCK_SB
 * OData V4 Service Binding
 *
 * Binding type : OData V4 – UI (Fiori)
 * Version      : 0001
 *
 * After activation click "Publish" in ADT to register
 * the service in the SAP S/4HANA Public Cloud service catalog.
 *
 * Service URL pattern (after publish):
 *   /sap/opu/odata4/sap/zmatl_daystock_sb/srvd/sap/
 *   zmatl_daystock_sd/0001/DayWiseMaterialStock
 */
